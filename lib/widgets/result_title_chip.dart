import 'package:flutter/material.dart';

class ResultTitleChip extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Chip(
      label: Text('8자 제목 칩'),
    );
  }
} 