import 'package:flutter/material.dart';

class PauseBanner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('일시정지 안내 배너'),
    );
  }
} 