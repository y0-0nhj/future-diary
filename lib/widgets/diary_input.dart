import 'package:flutter/material.dart';

class DiaryInput extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('미래일기 입력 위젯'),
    );
  }
} 