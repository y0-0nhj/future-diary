import 'package:flutter/material.dart';

class DiaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('미래일기 작성 화면')),
    );
  }
} 