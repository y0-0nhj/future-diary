import 'package:flutter/material.dart';

class PauseModeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('일시정지 모드/재개 화면')),
    );
  }
} 