import 'package:flutter/material.dart';

class PlanGenerateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('AI 계획 생성 화면')),
    );
  }
} 