import 'package:flutter/material.dart';

class PlanPreviewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('월별 계획 미리보기')),
    );
  }
} 