import 'package:flutter/material.dart';

class GoogleConnectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('구글 연동/권한 요청 화면')),
    );
  }
} 