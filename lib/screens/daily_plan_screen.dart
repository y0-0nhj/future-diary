import 'package:flutter/material.dart';

class DailyPlanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('일별 계획 상세/수정 화면')),
    );
  }
} 